spring:
  application:
    name: eureka-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8083
      profile: dev
           
eureka:
  server:
    renewal-percent-threshold: 0.85